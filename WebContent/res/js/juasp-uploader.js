/**
 * WebUploader 的封装jQuery组件.
 * 
 * Copyright 2015-2016 the original author or authors.
 * HomePage: http://www.kayura.org
 */


(function($, win) {

/*	var appPath = win.applicationPath === "" ? "" : win.applicationPath || "../..";
	
	function S4() {
		return (((1 + Math.random()) * 0x10000) | 0).toString(16).substring(1);
	}
	
	function _initWebUploader(item, options) {
		
		if(!WebUploader.Uploader.support()){
			var error = "上传控件不支持您的浏览器！请尝试升级flash版本或者使用Chrome引擎的浏览器。";
			if(win.console){
				win.console.log(error);
			}
			$(item).text(error);
			return;
		}
		
		var defaults = {
			hiddenInputId: "uploadifyHiddenInputId",
			onAllComplete: function (event) { },
			onComplete: function (event) { },
			innerOptions: {},
			fileNumLimit: undefined,
			fileSizeLimit: undefined,
			fileSingleSizeLimit: 1024 * 1024 * 4,
			PostbackHold: false
		};
		
		var opts = $.extend({}, defaults, options);
		
		var target = $(item);
		var pickerid = juasp.newid();
		
		var uploaderBody = '<div class="webuploader">' +
	        '<div id="thelist" class="uploader-list"></div>' +
	        '<div class="btns">' +
	        '<div id="' + pickerid + '">选择文件</div>' +
	        '</div>' +
	        '</div>';
		target.append(uploaderBody);
		
	}*/
	
}(jQuery, window));